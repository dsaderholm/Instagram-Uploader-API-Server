# Core dependencies
flask>=2.3.3
werkzeug>=2.3.7
instagrapi>=2.0.0

# Image and video processing
Pillow>=10.0.1
moviepy>=1.0.3
decorator>=5.1.1  # Required by moviepy
numpy>=1.24.0    # Required by moviepy
requests>=2.31.0  # Required by moviepy
proglog>=0.1.10   # Required by moviepy
tqdm>=4.66.1     # Required by moviepy